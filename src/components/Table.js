import { Label, Table } from 'semantic-ui-react';
import React, { useState } from 'react';

export default function MyTable(){
    const [text, setText] = useState('');
    return(<div>Table stays here</div>)
}